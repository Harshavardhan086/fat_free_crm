- if (params['controller'] == 'orders') and (template = template_for_current_view).present?
  = render(template, :order => order)
- else
  -# default view
  = render 'orders/index_brief', :order => order
